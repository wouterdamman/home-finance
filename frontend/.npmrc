# Dependency lifecycle scripts (preinstall/install/postinstall) are arbitrary
# code executed at install time by every package in the tree — on the CI runner
# and inside the Docker build alike. Nothing this app depends on needs them:
# the only package that asks is fsevents, which is darwin-only, optional, and
# unused by the Linux builds.
ignore-scripts=true
