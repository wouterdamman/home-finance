# Environment — glob, not a list: a future .env.prod / .env.staging must not be
# committable just because nobody remembered to add it here.
.env*
!.env.example

# Go
backend/bin/
backend/server
backend/importer
backend/internal/store/gen/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# Docker
*.log

# OS
.DS_Store
